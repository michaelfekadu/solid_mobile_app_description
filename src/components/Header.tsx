import Head from "next/head";

export default function Header() {
  return (
    <div>
      <Head>
        <title>Stryker Internship Project</title>

        <meta
          name="description"
          content="Solid mobile app project description."
        />
        <meta name="viewport" content="initial-scale=1.0, width=device-width" />
      </Head>
    </div>
  );
}
